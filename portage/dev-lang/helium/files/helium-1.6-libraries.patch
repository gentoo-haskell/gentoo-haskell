diff -Naur helium-1.6.orig/helium/src/Makefile.in helium-1.6/helium/src/Makefile.in
--- helium-1.6.orig/helium/src/Makefile.in	2006-02-05 17:34:46.000000000 +0100
+++ helium-1.6/helium/src/Makefile.in	2006-04-18 20:31:59.000000000 +0200
@@ -29,7 +29,8 @@
 prefix      = @prefix@
 exec_prefix = @exec_prefix@
 bindir      = @bindir@
-libdir      = @prefix@/helium/lib
+libdir      = @prefix@/helium/lib/simple
+tclibdir    = @prefix@/helium/lib
 demodir     = @prefix@/helium/demo
 
 status :
@@ -38,6 +39,8 @@
 	@echo "Type \"make install\" to install."
 	@echo "  Binaries will go into  $(bindir)"
 	@echo "  Libraries will go into $(libdir)"
+	@echo "  Libraries supporting type classes will go into"
+	@echo "							$(tclibdir)"
 	@echo "  Demos will go into     $(demodir)"
 	@echo "*******************************************************************"
 
@@ -429,10 +432,13 @@
 
 install : smaller
 	# *** Copy sources to library directory
-	$(MKDIR) $(bindir) $(libdir) $(demodir)
+	$(MKDIR) $(bindir) $(libdir) $(tclibdir) $(demodir)
 	$(INSTALL) -m 644 ../lib/simple/Prelude.hs $(libdir)
 	$(INSTALL) -m 644 ../lib/simple/List.hs $(libdir)
 	$(INSTALL) -m 644 ../lib/simple/Maybe.hs $(libdir)
+	$(INSTALL) -m 644 ../lib/Prelude.hs $(tclibdir)
+	$(INSTALL) -m 644 ../lib/List.hs $(tclibdir)
+	$(INSTALL) -m 644 ../lib/Maybe.hs $(tclibdir)
 	$(INSTALL) -m 755 $(HELIUMBINDIR)/$(MAIN)$(EXE) $(bindir)
 	$(INSTALL) -m 755 $(LVMRUNTIMEDIR)/lvmrun$(EXE) $(bindir)
 	$(INSTALL) -m 755 $(HELIUMBINDIR)/texthint$(EXE) $(bindir)
@@ -451,7 +457,17 @@
 	$(INSTALL) -m 644 ../lib/simple/Prelude.lvm $(libdir)
 	$(INSTALL) -m 644 ../lib/simple/PreludePrim.lvm $(libdir)
 	$(INSTALL) -m 644 ../lib/simple/Prelude.type $(libdir)
+	$(INSTALL) -m 644 ../lib/HeliumLang.lvm $(tclibdir)
+	$(INSTALL) -m 644 ../lib/List.lvm $(tclibdir)
+	$(INSTALL) -m 644 ../lib/LvmException.lvm $(tclibdir)
+	$(INSTALL) -m 644 ../lib/LvmIO.lvm $(tclibdir)
+	$(INSTALL) -m 644 ../lib/LvmLang.lvm $(tclibdir)
+	$(INSTALL) -m 644 ../lib/Maybe.lvm $(tclibdir)
+	$(INSTALL) -m 644 ../lib/Prelude.lvm $(tclibdir)
+	$(INSTALL) -m 644 ../lib/PreludePrim.lvm $(tclibdir)
+	$(INSTALL) -m 644 ../lib/Prelude.type $(tclibdir)
 	touch $(libdir)/*.lvm
+	touch $(tclibdir)/*.lvm
 
 movetohome: smaller
 	# *** Copying simple library sources to distribution directory
