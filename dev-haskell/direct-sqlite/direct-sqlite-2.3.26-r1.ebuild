# Copyright 1999-2022 Gentoo Authors
# Distributed under the terms of the GNU General Public License v2

EAPI=8

# ebuild generated by hackport 0.7.1.1.9999
# Tests don't pass with USE=systemlib, disabling for now
#hackport: flags: +haveusleep -systemlib

CABAL_FEATURES="lib profile haddock hoogle hscolour test-suite"
inherit haskell-cabal

DESCRIPTION="Low-level binding to SQLite3.  Includes UTF8 and BLOB support"
HOMEPAGE="https://github.com/IreneKnapp/direct-sqlite"
SRC_URI="https://hackage.haskell.org/package/${P}/${P}.tar.gz"

LICENSE="BSD"
SLOT="0/${PV}"
KEYWORDS="~amd64 ~x86"
IUSE="+fulltextsearch +json1 +urifilenames"

RDEPEND=">=dev-haskell/semigroups-0.18:=[profile?]
	>=dev-lang/ghc-8.4.3:=
	virtual/libc
"
DEPEND="${RDEPEND}
	>=dev-haskell/cabal-2.2.0.1
	test? ( dev-haskell/base16-bytestring
		dev-haskell/hunit
		dev-haskell/temporary )
"

src_prepare() {
	default

	cabal_chdeps \
		'semigroups >= 0.18 && < 0.20' 'semigroups >=0.18'
}

src_configure() {
	haskell-cabal_src_configure \
		$(cabal_flag fulltextsearch fulltextsearch) \
		--flag=haveusleep \
		$(cabal_flag json1 json1) \
		--flag=-systemlib \
		$(cabal_flag urifilenames urifilenames)
}
