# Copyright 1999-2020 Gentoo Authors
# Distributed under the terms of the GNU General Public License v2

EAPI=7

# ebuild generated by hackport 0.5.9999

CABAL_FEATURES="lib profile haddock hoogle hscolour"
inherit haskell-cabal

MY_PN="HaskellNet"
MY_P="${MY_PN}-${PV}"

DESCRIPTION="Client support for POP3, SMTP, and IMAP"
HOMEPAGE="https://github.com/jtdaugherty/HaskellNet"
SRC_URI="https://hackage.haskell.org/package/${MY_P}/${MY_P}.tar.gz"

LICENSE="BSD"
SLOT="0/${PV}"
KEYWORDS="~amd64 ~x86"
IUSE=""

RDEPEND="dev-haskell/base64-string:=[profile?]
	dev-haskell/cryptohash:=[profile?]
	>=dev-haskell/mime-mail-0.4.7:=[profile?]
	dev-haskell/mtl:=[profile?]
	>=dev-haskell/network-2.7:=[profile?] <dev-haskell/network-3.2:=[profile?]
	>=dev-haskell/network-bsd-2.7:=[profile?] <dev-haskell/network-bsd-2.9:=[profile?]
	dev-haskell/old-time:=[profile?]
	dev-haskell/text:=[profile?]
	>=dev-lang/ghc-7.4.1:=
"
DEPEND="${RDEPEND}
	>=dev-haskell/cabal-1.6
"

S="${WORKDIR}/${MY_P}"

src_prepare() {
	default
	# add Network.Compat to Other-modules:
	sed -i '/Other-modules:/ a\    Network\.Compat' "${MY_PN}.cabal"
	cabal_chdeps \
		'mime-mail >= 0.4.7 && < 0.5' 'mime-mail >= 0.4.7' \
		'network' 'network >= 2.7, network-bsd >= 2.7'
}

PATCHES=( "${FILESDIR}/${P}-network-3.patch" )
