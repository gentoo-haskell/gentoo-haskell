# Copyright 1999-2019 Gentoo Authors
# Distributed under the terms of the GNU General Public License v2

EAPI=7

# ebuild generated by hackport 0.6.9999
#hackport: flags: =-homebrew-openssl,-macports-openssl

CABAL_FEATURES="lib profile haddock hoogle hscolour test-suite"
inherit haskell-cabal

MY_PN="HsOpenSSL"
MY_P="${MY_PN}-${PV}"

DESCRIPTION="Partial OpenSSL binding for Haskell"
HOMEPAGE="https://github.com/vshabanov/HsOpenSSL"
SRC_URI="mirror://hackage/packages/archive/${MY_PN}/${PV}/${MY_P}.tar.gz"

LICENSE="public-domain"
SLOT="0/${PV}"
KEYWORDS="~amd64 ~x86"
IUSE="fast-bignum libressl"
REQUIRED_USE="fast-bignum? ( !libressl )"

RDEPEND=">=dev-haskell/network-2.1:=[profile?] <dev-haskell/network-3.1:=[profile?]
	>=dev-lang/ghc-7.10.1:=
	!libressl? (
		fast-bignum? ( <dev-libs/openssl-1.1.0:0= )
		!fast-bignum? ( dev-libs/openssl:0= )
	)
	libressl? ( dev-libs/libressl:= )
"
DEPEND="${RDEPEND}
	>=dev-haskell/cabal-1.22.2.0
"

S="${WORKDIR}/${MY_P}"

src_prepare() {
	default

	# As per http://hackage.haskell.org/package/HsOpenSSL-0.11.4.16/revisions/
	cabal_chdeps\
		'bytestring >= 0.9' 'bytestring >=0.9 && <0.11'\
		'network    >= 2.1' 'network >=2.1 && <3.1'\
		'time       >= 1.5' 'time >=1.5 && <1.10'
}

src_configure() {
	haskell-cabal_src_configure \
		$(cabal_flag fast-bignum fast-bignum) \
		--flag=-homebrew-openssl \
		--flag=-macports-openssl
}
