# Copyright 1999-2011 Gentoo Foundation
# Distributed under the terms of the GNU General Public License v2
# $Header:  $

# ebuild generated by hackport 0.2.13

EAPI="3"

CABAL_FEATURES="lib profile haddock hscolour"
inherit haskell-cabal

DESCRIPTION="Check properties on standard classes and data structures."
HOMEPAGE="http://haskell.org/haskellwiki/checkers"
SRC_URI="http://hackage.haskell.org/packages/archive/${PN}/${PV}/${P}.tar.gz"

LICENSE="BSD"
SLOT="0"
KEYWORDS="~amd64 ~x86"
IUSE=""

RDEPEND=">=dev-haskell/quickcheck-2.3
		>=dev-lang/ghc-6.8.2"
DEPEND="${RDEPEND}
		>=dev-haskell/cabal-1.2"

src_compile () {
	if has_version '>=dev-lang/ghc-7.0.4' && has_version '<dev-lang/ghc-7.2.0'; then
		has src_configure ${HASKELL_CABAL_EXPF} || haskell-cabal_src_configure "$@"
		unset LANG LC_ALL LC_MESSAGES
		# With ghc 7.0.4 the build fails twice then succeeds the third time
		./setup build
		./setup build
		./setup build \
			|| die "setup build failed"
	else
		cabal_src_compile
	fi
}

