# Copyright 1999-2022 Gentoo Authors
# Distributed under the terms of the GNU General Public License v2

EAPI=8

# ebuild generated by hackport 0.7.1.1.9999

CABAL_PN="HStringTemplate"
CABAL_P="${CABAL_PN}-${PV}"

CABAL_FEATURES="lib profile haddock hoogle hscolour test-suite"
CABAL_HACKAGE_REVISION="2"
inherit haskell-cabal

S="${WORKDIR}/${CABAL_P}"

DESCRIPTION="StringTemplate implementation in Haskell"
HOMEPAGE="https://hackage.haskell.org/package/HStringTemplate"
SRC_URI="https://hackage.haskell.org/package/${CABAL_P}/${CABAL_P}.tar.gz
	https://hackage.haskell.org/package/${CABAL_P}/revision/${CABAL_HACKAGE_REVISION}.cabal
		-> ${CABAL_P}-rev${CABAL_HACKAGE_REVISION}.cabal"

LICENSE="BSD"
SLOT="0/${PV}"
KEYWORDS="~amd64"

RDEPEND="<dev-haskell/blaze-builder-0.5:=[profile?]
	<dev-haskell/old-locale-1.1:=[profile?]
	>=dev-haskell/semigroups-0.16:=[profile?] <dev-haskell/semigroups-0.21:=[profile?]
	<dev-haskell/syb-0.8:=[profile?]
	<dev-haskell/void-0.8:=[profile?]
	>=dev-lang/ghc-8.4.3:=
"
DEPEND="${RDEPEND}
	>=dev-haskell/cabal-2.2.0.1
	test? ( >=dev-haskell/hunit-1.5
		>=dev-haskell/quickcheck-2.13
		>=dev-haskell/random-1.0 )
"
BDEPEND="app-text/dos2unix"

src_prepare() {
	# pull revised cabal from upstream
	cp "${DISTDIR}/${CABAL_P}-rev${CABAL_HACKAGE_REVISION}.cabal" "${S}/${CABAL_PN}.cabal" || die

	# Convert to unix line endings
	dos2unix "${S}/${CABAL_PN}.cabal" || die

	# Apply patches *after* pulling the revised cabal
	default
}
