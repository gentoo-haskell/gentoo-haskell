# Copyright 1999-2019 Gentoo Authors
# Distributed under the terms of the GNU General Public License v2

EAPI=7

# ebuild generated by hackport 0.5.6.9999
#hackport: flags: +systemlib

CABAL_FEATURES="lib profile haddock hoogle hscolour test-suite"
inherit haskell-cabal

DESCRIPTION="Backend for the persistent library using sqlite3"
HOMEPAGE="http://www.yesodweb.com/book/persistent"
SRC_URI="https://hackage.haskell.org/package/${P}/${P}.tar.gz"

LICENSE="MIT"
SLOT="0/${PV}"
KEYWORDS="~amd64 ~x86"
IUSE="system-sqlite build-sanity-exe +full-text-search +have-usleep +json1 +uri-filenames"

RDEPEND=">=dev-haskell/aeson-0.6.2:=[profile?]
	>=dev-haskell/conduit-1.2.8:=[profile?]
	>=dev-haskell/microlens-th-0.4.1.1:=[profile?]
	>=dev-haskell/monad-logger-0.2.4:=[profile?]
	dev-haskell/old-locale:=[profile?]
	>=dev-haskell/persistent-2.9:=[profile?] <dev-haskell/persistent-3:=[profile?]
	dev-haskell/resource-pool:=[profile?]
	>=dev-haskell/resourcet-1.1:=[profile?]
	>=dev-haskell/text-0.7:=[profile?]
	dev-haskell/unliftio-core:=[profile?]
	dev-haskell/unordered-containers:=[profile?]
	>=dev-lang/ghc-7.10.1:=
	virtual/libc
	system-sqlite? ( >=dev-db/sqlite-3.0 )
"
DEPEND="${RDEPEND}
	>=dev-haskell/cabal-1.22.2.0
	test? ( dev-haskell/hspec
		dev-haskell/persistent-template
		dev-haskell/temporary )
	system-sqlite? ( virtual/pkgconfig )
"

src_configure() {
	haskell-cabal_src_configure \
		$(cabal_flag build-sanity-exe build-sanity-exe) \
		$(cabal_flag full-text-search full-text-search) \
		$(cabal_flag have-usleep have-usleep) \
		$(cabal_flag json1 json1) \
		$(cabal_flag system-sqlite systemlib) \
		$(cabal_flag system-sqlite use-pkgconfig) \
		$(cabal_flag uri-filenames uri-filenames)
}
