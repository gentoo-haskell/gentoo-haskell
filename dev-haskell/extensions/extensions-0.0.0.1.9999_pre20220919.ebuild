# Copyright 1999-2022 Gentoo Authors
# Distributed under the terms of the GNU General Public License v2

EAPI=8

# ebuild generated by hackport 0.7.3.0

CABAL_PV="0.0.0.1.9999"

CABAL_FEATURES="lib profile haddock hoogle hscolour test-suite"
inherit haskell-cabal

DESCRIPTION="Parse Haskell Language Extensions"
HOMEPAGE="https://github.com/kowainik/extensions"

REPO_NAME="extensions"
REPO_URI="https://github.com/kowainik/${REPO_NAME}"
COMMIT="ab02b7b017a8ec301ba0320276383c5a618765c0"
BRANCH="main"
SRC_URI="${REPO_URI}/archive/${COMMIT}.tar.gz -> ${REPO_NAME}-${COMMIT}.tar.gz"
S="${WORKDIR}/${REPO_NAME}-${COMMIT}"
CABAL_FILE="${S}/${PN}.cabal"

LICENSE="MPL-2.0"
SLOT="0/${PV}"
KEYWORDS="~amd64 ~x86"
IUSE="executable"

PATCHES=(
	"${FILESDIR}/${PN}-0.0.0.1.9999-add-executable-flag.patch"
	"${FILESDIR}/${PN}-0.0.0.1.9999-cabal-3_6.patch"
)

CABAL_CHDEPS=(
	'version:             0.0.0.1' 'version:             0.0.0.1.9999'
	'optparse-applicative >= 0.15 && < 0.17' 'optparse-applicative >= 0.15'
)

RDEPEND="
	>=dev-haskell/cabal-3.0:=[profile?] <dev-haskell/cabal-3.7:=[profile?]
	>=dev-lang/ghc-8.8.1:=
	executable? (
		>=dev-haskell/colourista-0.1:=[profile?] <dev-haskell/colourista-0.3:=[profile?]
		>=dev-haskell/optparse-applicative-0.15:=[profile?]
	)
"
DEPEND="${RDEPEND}
	>=dev-haskell/cabal-3.0.0.0
	test? (
		>=dev-haskell/hedgehog-1.0 <dev-haskell/hedgehog-1.1
		dev-haskell/hspec
		>=dev-haskell/hspec-hedgehog-0.0.1 <dev-haskell/hspec-hedgehog-0.1
	)
"

src_configure() {
	haskell-cabal_src_configure \
		$(cabal_flag executable executable)
}
