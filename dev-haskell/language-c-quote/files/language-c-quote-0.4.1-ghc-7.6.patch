--- language-c-quote-0.4.1-orig/language-c-quote.cabal	2012-08-16 18:33:41.000000000 +1000
+++ language-c-quote-0.4.1/language-c-quote.cabal	2012-09-14 20:27:15.202649013 +1000
@@ -24,12 +24,12 @@
   build-depends:
     array >=0.2 && <0.5,
     base >=4 && <5,
-    bytestring >=0.9 && <0.10,
-    containers >=0.4 && <0.5,
+    bytestring >=0.9 && <0.11,
+    containers >=0.4 && <0.6,
     exception-mtl >=0.3 && <0.4,
     exception-transformers >=0.3 && <0.4,
     filepath >=1.2 && <1.4,
-    haskell-src-meta >= 0.4 && < 0.6,
+    haskell-src-meta >= 0.4 && < 0.7,
     mainland-pretty ==0.2.*,
     mtl >=2.0 && <3,
     srcloc == 0.2.*,
@@ -40,9 +40,9 @@
     build-depends:
       template-haskell >=2.5 && <2.6
 
-  if impl(ghc >= 7.4) && impl(ghc < 7.6)
+  if impl(ghc >= 7.4) && impl(ghc < 7.8)
     build-depends:
-      template-haskell >=2.7 && <2.8
+      template-haskell >=2.7 && <2.9
 
   if impl(ghc < 7.4)
     build-tools:
