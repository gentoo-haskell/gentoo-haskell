# Copyright 1999-2013 Gentoo Foundation
# Distributed under the terms of the GNU General Public License v2
# $Header: $

EAPI=5

# ebuild generated by hackport 0.3.4.9999

CABAL_FEATURES="lib profile haddock hoogle hscolour"
inherit flag-o-matic haskell-cabal

DESCRIPTION="Accelerate backend for NVIDIA GPUs"
HOMEPAGE="https://github.com/AccelerateHS/accelerate-cuda/"
SRC_URI="mirror://hackage/packages/archive/${PN}/${PV}/${P}.tar.gz"

LICENSE="BSD"
SLOT="0/${PV}"
KEYWORDS="~amd64 ~x86"
IUSE="+bounds-checks debug internal-checks unsafe-checks"

RDEPEND=">=dev-haskell/accelerate-0.13:=[profile?] <dev-haskell/accelerate-0.14:=[profile?]
	>=dev-haskell/binary-0.5:=[profile?] <dev-haskell/binary-0.8:=[profile?]
	>=dev-haskell/cryptohash-0.7:=[profile?] <dev-haskell/cryptohash-0.11:=[profile?]
	>=dev-haskell/cuda-0.5.0.2:=[profile?] <dev-haskell/cuda-0.6:=[profile?]
	>=dev-haskell/fclabels-1.0:=[profile?] <dev-haskell/fclabels-1.2:=[profile?]
	>=dev-haskell/hashable-1.1:=[profile?] <dev-haskell/hashable-1.3:=[profile?]
	>=dev-haskell/hashtables-1.0:=[profile?] <dev-haskell/hashtables-1.2:=[profile?]
	>=dev-haskell/language-c-quote-0.4.4:=[profile?] <dev-haskell/language-c-quote-0.8:=[profile?]
	>=dev-haskell/mainland-pretty-0.2:=[profile?] <dev-haskell/mainland-pretty-0.3:=[profile?]
	>=dev-haskell/mtl-2.0:=[profile?] <dev-haskell/mtl-2.2:=[profile?]
	>=dev-haskell/safesemaphore-0.9:=[profile?] <dev-haskell/safesemaphore-0.10:=[profile?]
	>=dev-haskell/srcloc-0.2:=[profile?] <dev-haskell/srcloc-0.5:=[profile?]
	>=dev-haskell/text-0.11:=[profile?] <dev-haskell/text-0.12:=[profile?]
	>=dev-haskell/transformers-0.2:=[profile?] <dev-haskell/transformers-0.4:=[profile?]
	>=dev-haskell/unordered-containers-0.1.4:=[profile?] <dev-haskell/unordered-containers-0.3:=[profile?]
	>=dev-lang/ghc-6.12.1:=
"
DEPEND="${RDEPEND}
	>=dev-haskell/cabal-1.8.0.2
"

src_prepare() {
	cabal_chdeps \
		'binary                  >= 0.5      && < 0.7' 'binary                  >= 0.5      && < 0.8' \
		'cryptohash              >= 0.7      && < 0.10' 'cryptohash              >= 0.7      && < 0.11'
}

src_configure() {
	# It assumes that configure is run, but does not run it (Andres has
	# patched upstream to fix this).  So need to run configure.  But its
	# difficult to make the configure works as it wants to use
	# /opt/cuda/bin/nvcc which does not understand lots
	# of g++ flags.
	strip-flags
	filter-flags -march* -ggdb -pipe -Wl* -fvar* -gdwarf* -Wa*
	haskell-cabal_src_configure \
		$(cabal_flag bounds-checks bounds-checks) \
		$(cabal_flag debug debug) \
		$(cabal_flag internal-checks internal-checks) \
		$(cabal_flag unsafe-checks unsafe-checks)
}
