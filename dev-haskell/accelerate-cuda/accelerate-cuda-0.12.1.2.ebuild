# Copyright 1999-2012 Gentoo Foundation
# Distributed under the terms of the GNU General Public License v2
# $Header: $

EAPI=5

# ebuild generated by hackport 0.3.1.9999

CABAL_FEATURES="lib profile haddock hoogle hscolour"
inherit base flag-o-matic haskell-cabal

DESCRIPTION="Accelerate backend for NVIDIA GPUs"
HOMEPAGE="http://www.cse.unsw.edu.au/~chak/project/accelerate/"
SRC_URI="mirror://hackage/packages/archive/${PN}/${PV}/${P}.tar.gz"

LICENSE="BSD"
SLOT="0/${PV}"
KEYWORDS="~amd64 ~x86"
IUSE=""

RDEPEND=">=dev-haskell/accelerate-0.12.1:=[profile?]
		<dev-haskell/accelerate-0.13:=[profile?]
		>=dev-haskell/binary-0.5:=[profile?]
		>=dev-haskell/cryptohash-0.7:=[profile?]
		>=dev-haskell/cuda-0.4.1:=[profile?]
		<dev-haskell/cuda-0.5:=[profile?]
		>=dev-haskell/fclabels-1.0:=[profile?]
		>=dev-haskell/hashable-1.1:=[profile?]
		>=dev-haskell/hashtables-1.0.1:=[profile?]
		>=dev-haskell/language-c-quote-0.4:=[profile?]
		>=dev-haskell/mainland-pretty-0.2:=[profile?]
		>=dev-haskell/mtl-2.0:=[profile?]
		>=dev-haskell/srcloc-0.2:=[profile?]
		>=dev-haskell/text-0.11:=[profile?]
		>=dev-haskell/transformers-0.2:=[profile?]
		>=dev-haskell/unordered-containers-0.1.4:=[profile?]
		>=dev-lang/ghc-7.0.1:="
DEPEND="${RDEPEND}
		>=dev-haskell/cabal-1.6"

PATCHES=("${FILESDIR}/${PN}-0.12.1.2-hashable-1.2.patch")

src_configure() {
	# It assumes that configure is run, but does not run it (Andres has
	# patched upstream to fix this).  So need to run configure.  But its
	# difficult to make the configure works as it wants to use
	# /opt/cuda/bin/nvcc which does not understand lots
	# of g++ flags.
	strip-flags
	filter-flags -march* -ggdb -pipe -Wl*
	haskell-cabal_src_configure
}
